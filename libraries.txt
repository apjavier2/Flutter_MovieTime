Libraries used for this application:
1. http - This is used to perform http methods and call the APIs (get data)
2. dotenv - This is used to protect the api key and base url (The .env file is added on the repository so that the application can be tested by technical team)